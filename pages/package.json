{
  "name": "pages",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev --host --allowedHosts",
    "start": "astro dev --host --allowedHosts",
    "build": "astro build",
    "preview": "astro preview --host --allowedHosts",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.37.1",
    "astro": "^5.6.1",
    "sharp": "^0.34.2",
    "vite": "^7.3.0"
  }
}
